<div .container-fluid .tagscrollpane>
  $case eResult
    $of Right tagvalues
      $forall tag <- tags
        <div .row>
          <div .col-xs-6>
            <label for=#{idAttr}>#{tag}
          <div .col-xs-6>
            <select id=#{idAttr} name=#{nameAttr} *{otherAttrs} :isReq:required .form-control>
              $# FIXME: Why a lookup here?
              $maybe tagvalue <- lookup tag tagvalues
                  $case tagvalue
                    $of TagInclude
                      <option selected>include
                      <option>exclude
                      <option>doesn't matter
                    $of TagExclude
                      <option>include
                      <option selected>exclude
                      <option>doesn't matter
                    $of TagIgnore
                      <option>include
                      <option>exclude
                      <option selected>doesn't matter
    $of Left _
      $forall tag <- tags
        <div .row>
          <div .col-xs-6>
            <label for=#{idAttr}>#{tag}
          <div .col-xs-6>
            <select id=#{idAttr} name=#{nameAttr} *{otherAttrs} :isReq:required .form-control>
              <option>include
              <option>exclude
              <option selected>doesn't matter
